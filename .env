# Default Docker Compose startup track for this repository.
# Plain `docker compose up -d` will bring up the PostgreSQL GHCR stack,
# including Kafka and ClickHouse analytics infrastructure.
COMPOSE_PROFILES=postgres-ghcr
