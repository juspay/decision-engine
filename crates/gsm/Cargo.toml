[package]
name = "gsm"
version = "0.1.0"
edition = "2021"
rust-version = "1.85.0"
description = "Gateway Status Map — connector error codes mapped to routing decisions"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
csv = "~1.3"
thiserror = "2.0"
