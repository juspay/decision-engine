-- Your SQL goes here
CREATE TABLE routing_algorithm_mapper (
    id VARCHAR(255) NOT NULL PRIMARY KEY,
    created_by VARCHAR(255) NOT NULL PRIMARY KEY,
    routing_algorithm_id VARCHAR(255) NOT NULL
);
